import 'package:flutter/material.dart';

class AddQuotationScreen extends StatelessWidget {
  const AddQuotationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Add Quote');
  }
}
